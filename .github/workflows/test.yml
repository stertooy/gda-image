name: Publish image

on:
  workflow_dispatch:
    inputs:
      BRANCH:
        required: true
        type: choice
        options:
          - "4.9-slim"
          - "4.9"

jobs:
  build:
    name: Publish GAP ${{ inputs.BRANCH }}
    runs-on: ubuntu-latest
    steps:
      - name: echoooo
        shell: bash
        run: |
          echo "${{ inputs.BRANCH }}"
